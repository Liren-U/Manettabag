---
/**
 * Reservation Section (VIP)
 * - Text Updated with multi-paragraph layout
 * - Standalone Button (No Container)
 * - Style: Luminous Fluid
 */
const BRAND_HEX = "#D46A22";
---

<style define:vars={{ brandColor: BRAND_HEX }}>
  /* 全局选中色 */
  .reservation-container ::selection {
    background-color: var(--brandColor);
    color: white;
  }

  /* ✨ 按钮样式：自发光流体质感 */
  .vip-pay-btn {
    position: relative;
    overflow: hidden;
    width: 100%; /* 强制全宽 */
    
    /* 1. 背景 */
    background: linear-gradient(135deg, #FF8F45 0%, #D46A22 100%);
    
    /* 2. 核心：去掉边框 */
    border: none; 
    
    /* 3. 混合阴影 */
    box-shadow: 
      inset 0 2px 0 rgba(255, 255, 255, 0.3),
      inset 0 -2px 0 rgba(0, 0, 0, 0.1),
      0 8px 30px rgba(212, 106, 34, 0.4),
      0 0 0 1px rgba(255, 255, 255, 0.05);

    transition: all 0.4s cubic-bezier(0.25, 0.8, 0.25, 1);
    z-index: 1;
  }

  /* 悬停状态 */
  .vip-pay-btn:hover {
    transform: translateY(-2px) scale(1.01);
    background: linear-gradient(135deg, #FFA060 0%, #E67325 100%);
    box-shadow: 
      inset 0 2px 0 rgba(255, 255, 255, 0.5),
      0 15px 50px rgba(212, 106, 34, 0.7), 
      0 0 20px rgba(255, 140, 60, 0.4);
  }

  /* 点击状态 */
  .vip-pay-btn:active {
    transform: scale(0.98);
    box-shadow: 
      inset 0 2px 5px rgba(0, 0, 0, 0.2),
      0 5px 20px rgba(212, 106, 34, 0.4);
  }

  /* ✨ 流光动画 */
  .vip-pay-btn::after {
    content: "";
    position: absolute;
    top: 0;
    left: -150%;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      90deg,
      transparent,
      rgba(255, 255, 255, 0.4),
      transparent
    );
    transform: skewX(-25deg);
    pointer-events: none;
    mix-blend-mode: overlay;
    animation: shine 5s infinite;
  }

  @keyframes shine {
    0% { left: -150%; }
    20% { left: 150%; }
    100% { left: 150%; }
  }
</style>

<section class="reservation-container flex-1 w-full pt-32 pb-20 lg:pt-40 lg:pb-32 relative overflow-hidden bg-[#050505] text-white antialiased">

  <div class="absolute top-[-10%] right-[-10%] w-[600px] h-[600px] bg-[var(--brandColor)] opacity-20 blur-[150px] rounded-full pointer-events-none"></div>
  <div class="absolute bottom-0 left-[-10%] w-[600px] h-[600px] bg-white/5 blur-[120px] rounded-full pointer-events-none"></div>

  <div class="max-w-[1200px] mx-auto px-6 lg:px-14 relative z-10">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-24 items-center mb-24">

      <div class="order-2 lg:order-1 mt-8 lg:mt-0">
        <img
          src="/images/ManettaBag-reservation-2.png"
          alt="Manetta Bag"
          class="w-full h-auto object-cover block"
        />
      </div>

      <div class="flex flex-col items-start order-1 lg:order-2 w-full">

        <div class="inline-flex items-center gap-2 px-3 py-1.5 rounded-full bg-white/5 border border-white/10 mb-8 backdrop-blur-md">
          <span class="relative flex h-2 w-2">
            <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-[var(--brandColor)] opacity-75"></span>
            <span class="relative inline-flex rounded-full h-2 w-2 bg-[var(--brandColor)]"></span>
          </span>
          <span class="text-[10px] font-bold tracking-[0.2em] text-gray-300">
            LIMITED TIME OFFER
          </span>
        </div>

        <h1 class="font-display font-bold text-3xl lg:text-6xl leading-[1.1] mb-6 text-white tracking-tight">
          Save $50 <span class="text-[#666] lg:text-2xl">vs Early Bird</span><br />
          <span class="text-[#666] lg:text-5xl">as a VIP backer.</span>
        </h1>

        <div class="text-base lg:text-lg text-gray-300 leading-relaxed mb-10 max-w-xl font-light space-y-6">
          <p>
            Join VIP for just <strong class="text-white font-medium">$1</strong> and unlock our biggest discount.<br />
            <span class="text-[var(--brandColor)] font-medium">Limited to 100 spots.</span> VIP access closes when we launch.
          </p>
          <p>
            After campaign launched, VIP who back the camera bag will also receive <span class="text-white/80">priority shipping</span>, a free lens protection Velcro cloth, and a complimentary S-size camera bag.
          </p>
          <p>
            On launch day, you’ll receive a private email with your exclusive VIP discount link.
          </p>
        </div>

        <a
          href="/vip-subscribe"
          class="vip-pay-btn group flex w-full items-center justify-center gap-3 px-12 py-5 rounded-2xl font-bold text-white text-lg tracking-wide mb-8"
          aria-label="Pay now and go to VIP subscribe page"
        >
          <span class="relative z-10 drop-shadow-[0_2px_2px_rgba(0,0,0,0.25)]">Pay Now</span>
          
          <svg class="w-5 h-5 relative z-10 transition-transform duration-300 group-hover:translate-x-1 drop-shadow-sm" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
            <path d="M5 12h14"></path>
            <path d="M13 6l6 6-6 6"></path>
          </svg>
        </a>

      </div>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
      <div class="bg-[#141414] border border-white/10 rounded-3xl p-8 hover:bg-[#1a1a1a] transition-colors duration-300">
        <h3 class="font-display font-bold text-lg text-white mb-2">100% Refund Guarantee</h3>
        <p class="text-gray-400 text-sm leading-relaxed">
          If you're not satisfied, we'll offer a full refund at any time.
        </p>
      </div>

      <div class="bg-[#141414] border border-white/10 rounded-3xl p-8 hover:bg-[#1a1a1a] transition-colors duration-300">
        <h3 class="font-display font-bold text-lg text-white mb-2">Transparency Guarantee</h3>
        <p class="text-gray-400 text-sm leading-relaxed">
          Stay informed about every project milestone and production stage.
        </p>
      </div>

      <div class="bg-[#141414] border border-white/10 rounded-3xl p-8 hover:bg-[#1a1a1a] transition-colors duration-300">
        <h3 class="font-display font-bold text-lg text-white mb-2">Secure Payment</h3>
        <p class="text-gray-400 text-sm leading-relaxed">
          All transactions are encrypted. We prioritize and respect your privacy.
        </p>
      </div>
    </div>

  </div>
</section>