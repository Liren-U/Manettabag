---
/**
 * ManettaBag-Feature-Comfort.astro
 * 第二个功能区块：Carries Heavy. Feels Light.
 */
---

<section class="py-40 bg-dark-sec relative">
  <!-- 顶部径向渐变背景 -->
  <div
    class="absolute inset-0 bg-[radial-gradient(ellipse_at_top,_var(--tw-gradient-stops))] from-brand/10 via-transparent to-transparent opacity-50 pointer-events-none"
  ></div>

  <div class="max-w-limit mx-auto px-6 lg:px-gutter relative z-10">
    <div class="grid grid-cols-1 lg:grid-cols-12 gap-20 items-center">
      <!-- 左侧文案 -->
      <div class="lg:col-span-5 reveal active order-2 lg:order-1">
        <h2 class="font-display font-bold text-5xl mb-8 leading-tight">
          Carries Heavy.<br />
          Feels Light.
        </h2>

        <p class="text-light-muted text-xl leading-relaxed mb-10">
          <span class="font-semibold italic"> Air-Mesh </span>
           back panel keeps you cool. Chest and shoulder straps
            balance the load—so 20kg feels like 10kg.
             Top grab handle makes carrying even easier.
        </p>

        <div class="flex gap-6">
          <div
            class="px-8 py-6 bg-dark-main/50 backdrop-blur-lg rounded-2xl border border-white/5 text-center flex-1"
          >
            <div class="text-4xl font-bold text-brand mb-1">2x</div>
            <div
              class="text-sm text-light-muted uppercase tracking-wider"
            >
              Airflow
            </div>
          </div>

          <div
            class="px-8 py-6 bg-dark-main/50 backdrop-blur-lg rounded-2xl border border-white/5 text-center flex-1"
          >
            <div class="text-4xl font-bold text-brand mb-1">24h</div>
            <div
              class="text-sm text-light-muted uppercase tracking-wider"
            >
              Comfort
            </div>
          </div>
        </div>
      </div>

      <!-- 右侧大图 -->
      <div class="lg:col-span-7 reveal active delay-100 order-1 lg:order-2">
        <div
          class="group relative rounded-[3rem] overflow-hidden bg-dark-main border border-white/10 shadow-2xl"
        >
          <img
            src="/images/Manetta-herobanner-product.png"
            alt="Comfort"
            class="w-full h-auto transform group-hover:scale-105 group-hover:-rotate-1 transition-all duration-1000 ease-out"
          />
        </div>
      </div>
    </div>
  </div>
</section>
